% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit_mvbcf_multi}
\alias{fit_mvbcf_multi}
\title{Fit a multi-treatment multivariate Bayesian Causal Forest}
\usage{
fit_mvbcf_multi(
  data,
  responses,
  treatments,
  covariates = NULL,
  order = 2L,
  gen = NULL,
  env = NULL,
  n_iter = 1500,
  n_burn = 750,
  keep_every = 2,
  n_tree = 100,
  n_tree_tau = 50,
  min_nodesize = 5,
  sd_control = 1,
  sd_effect = 0.5,
  tree_iters = NULL,
  seed = 1,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data frame, one row per unit.}

\item{responses}{Character vector of response columns (q >= 1).}

\item{treatments}{Character vector of binary (0/1) treatment columns.}

\item{covariates}{Moderator/control columns. If \code{NULL}, all numeric
columns except responses, treatments, ids and any \code{true_*}/ground-truth
columns are used.}

\item{order}{Maximum interaction order (default 2 = up to pairwise).}

\item{gen, env}{Optional genotype / environment id columns (MET use). When
given, genotype is one-hot encoded (prefix \code{gen_}) into the moderators
and a full gen x env prediction grid is built.}

\item{n_iter, n_burn, keep_every, n_tree, n_tree_tau, min_nodesize}{MCMC/forest
settings. \code{n_tree} is the prognostic forest size; \code{n_tree_tau} is
used for every effect forest.}

\item{sd_control, sd_effect}{Prior scale multipliers: node prior covariance is
\code{diag(sd_control^2 / n_tree)} for \eqn{\mu} and
\code{diag(sd_effect^2 / n_tree_tau)} for every effect forest (Hahn-style
half-scale on effects).}

\item{tree_iters}{Iterations whose tree structure is stored (bartMan). Default
last 20.}

\item{seed}{Seed for the propensity models.}

\item{verbose}{Print C++ progress.}
}
\value{
An object of class \code{"mvbcfMT"}.
}
\description{
Fits the additive factorial MVBCF
\deqn{Y_i = \mu(x_i) + \sum_{\emptyset \ne S,\,|S|\le r}
       \Big(\prod_{t\in S} Z_{it}\Big)\, \tau_S(x_i) + \varepsilon_i,
       \quad \varepsilon_i \sim N_q(0,\Sigma),}
where each \eqn{\mu} and \eqn{\tau_S} is its own multivariate sum-of-trees
forest. With two treatments and \code{order = 2} this is the
\eqn{(\mu,\tau_1,\tau_2,\tau_{12})} factorial model; \code{order} truncates
the interaction lattice for more than two treatments.
}
\examples{
\donttest{
sim <- simulate_multi(n = 150, q = 2, Ti = 2, seed = 1)
fit <- fit_mvbcf_multi(sim$data, responses = sim$responses,
                       treatments = sim$treatments, covariates = sim$covariates,
                       n_iter = 100, n_burn = 50, n_tree = 30, n_tree_tau = 20)
print(fit)
}
}
